# E22 / Gap6 Verify — 2026-09-07

Human cue: 「幫我驗E22／Gap6」  
Generated: `2026-09-07T09:09:02+00:00`  
Soft-Frozen: **[0.50, 0.95] KEEP** — no history rewrite

## Preconditions

| Check | Result |
|---|---|
| Monday schedule forward landed? | **NO** (as of verify time) |
| Last live session | **2026-09-04** |
| Cron expected | `30 8 * * 1-5` UTC (~16:30 Taipei); recent schedules often land ~12:50–13:30 UTC |
| Manual `workflow_dispatch` | **403** (integration cannot dispatch) |

## Runbook results (`POST_FORWARD_E22_VERIFY_RUNBOOK`)

| Step | Exit | Verdict |
|---|---:|---|
| `e21_qc.py` | 0 | **PASS** · Exact T+1 ok · Soft-Frozen clip ok |
| `e22_gap6_fidelity_kpi.py` | 2 | **BLOCKED** live evidence |
| `e22_data_quality_kpi.py` | 0 | **PASS** · kpi_ok |
| `ops_alert_scan.py --report-only` | 0 | overall **HIGH** · CRITICAL **0** |

## Gap6 live ledger detail

| Field | Present |
|---|---|
| `portfolio_state.e22_books_version` | **YES** (`E22_v2s_tw`) |
| `portfolio_state.e22_manifest` | **YES** |
| `nav.csv` column `e22_version` | **NO** ← blocks `live_ledger_e22_fields_present` |
| `dividends_applied.csv` | **NO** (tax sensitivity deferred; not required for fields_present) |

Flags still raised:

- `LIVE_LEDGER_E22_FIELDS_MISSING`
- `KPI_BLOCKED_LIVE_EVIDENCE_MISSING`

Code wire: **`code_ok=true`** (DEFAULT `E22_v2s_tw` · e21 imports/apply wired).

## Why not green yet

Weekend push re-run (2026-09-06) wrote E22 keys into `portfolio_state.json` but did **not** append a new `nav.csv` row (no new TW session), so `e22_version` column never appeared. Gap6 requires that column. First **weekday** forward that appends a new session row should clear the flag (via `append_immutable` adding the column).

## Pass criteria vs now

| Criterion | Now |
|---|---|
| QC PASS / Exact T+1 | **YES** |
| Ledger keys books_version + manifest | **YES** |
| `nav.e22_version` | **NO** |
| Gap6 missing-flag cleared | **NO** |
| Soft-Frozen unchanged | **YES** |

## Next action

1. Wait for Monday GHA `v412f-forward-paper` schedule (or human dispatch).
2. Confirm `forward/e21/nav.csv` gains `e22_version` and `last_date` advances (expect **2026-09-07** if TW traded).
3. Re-run this runbook; update `LIVE_E22_FIELD_EVIDENCE.md` → **EVIDENCE PRESENT** only if Gap6 clears.
4. Do **not** backfill / rewrite historical rows.

Label: `E22_GAP6_VERIFY_2026-09-07__PRE_MONDAY_FORWARD__QC_PASS_GAP6_BLOCKED_NAV_COL`
